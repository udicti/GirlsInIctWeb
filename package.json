{
  "name": "functions",
  "version": "0.2.0",
  "description": "smart girl in ict platform",
  "scripts": {
    "start": "bfast fs serve",
    "deploy": "bfast functions deploy --token ${BFAST_TOKEN} --projectId ${PROJECT_ID}"
  },
  "engines": {},
  "dependencies": {
    "bfastnode": "^4.5.3",
    "cookie-parser": "^1.4.4",
    "express": "^4.17.1"
  },
  "devDependencies": {
  },
  "private": true
}
